services:
  nginx:
    image: nginx:latest
    container_name: stayease_nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    networks:
      - stayease_network

networks:
  stayease_network:
    external: true